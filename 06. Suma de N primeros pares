#include <iostream>
using namespace std;
int main() {
    int N,i;
    int suma = 0;
    cin >> N;
    for ( i = 1; i <= N; ++i) {
        suma=suma + (2 * i);
    }
    cout << suma << endl;
    return 0;
}
