#include <iostream>
using namespace std;
int main() {
  int suma = 0;
  for (int i = 1; i <= 3; i++) { 
    for (int j = 2; j <= 4; j++) { 
      suma=suma + (i + j);
    }
  }
  cout << suma << endl;
  return 0;
}
